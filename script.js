function toggleMenu() {
    const menu=document.quarySelector(".menu-links");
    const icon=document.quarySelector(".hamburger-icon");
    menu.classList.toggle("open");
    icon.classList.toggle("open");
}